# Стего Студия

Desktop-проект для LSB-стеганографии с основным интерфейсом на `PySide6`.

## Запуск (основной UI)

```bash
cd /Users/ryltsin/Documents/GitHub/dictor/steganography-contest
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python main_qt.py
```

## Тесты

```bash
cd /Users/ryltsin/Documents/GitHub/dictor/steganography-contest
python -m unittest discover -s tests -p 'test_*.py'
```

## Структура

- `main_qt.py` — основной запуск нового UI.
- `ui_qt/` — Qt-интерфейс (тема, строки, диалоги, главное окно).
- `core/stego.py` — encode/decode и проверки емкости.
- `core/risk.py` — chi-square и оценка риска LOW/MEDIUM/HIGH.
- `core/analysis.py` — heatmap, симулятор атак, автосравнение режимов.
- `core/report.py` — отчёт схемы `stegano_report/1.1.0`.
- `core/proof_pack.py` — экспорт zip-пакета с артефактами.
- `docs/report_template_v1_1.json` — шаблон отчёта версии 1.1.

## Что есть в новом UI

- Полностью русскоязычный интерфейс.
- Режимы интерфейса:
  - `Базовый` — упрощённый экран для повседневной работы.
  - `Эксперт` — полный набор аналитики, hotspot и пиксельный инспектор.
- Сценарии: открыть, скрыть, извлечь, сохранить, отчёт, архив данных.
- Живой аналитический HUD прямо в главном окне:
  - процент изменённых пикселей;
  - средняя и максимальная дельта;
  - порог чувствительности;
  - быстрый/full-res режим анализа.
- Пиксельный инспектор:
  - координаты;
  - значения `R/G/B` до и после;
  - двоичные представления каналов;
  - `LSB` до/после;
  - мини-лупа `12x/24x`.
- Hotspot-карта изменений с кликом по тайлу для перехода к проблемной области.
- Экспорт отчёта теперь сохраняет рядом PNG-скриншоты:
  - `*_hotspot.png`
  - `*_inspector.png`
- Окно `До/После + Теплокарта` с режимами split/blend/heatmap и миганием слоёв.
- Встроенный в главное окно анализ с режимами:
  - `Разделение`
  - `Смешивание`
  - `Теплокарта`
  - `Усиление ×20`
- `Симулятор атак` (таблица устойчивости).
- `Сравнение режимов` (`method × bits`, метрики качества).
- Демо-режим одной кнопкой.
- `Демо за 2 минуты`: пошаговый таймлайн автосценария + итоговый экран.
- Предпросмотр отчёта с вкладками: `TXT`, `JSON`, `Сводка`.
- Кнопки-пиктограммы:
  - `☀/🌙` — мгновенное переключение темы;
  - `❔` — подробная справка по программе и LSB.

## Архив данных

Экспортирует детерминированный ZIP со структурой:

- `proof_pack/report.json`
- `proof_pack/report.txt`
- `proof_pack/before.png`
- `proof_pack/after.png`
- `proof_pack/heatmap.png`
- `proof_pack/hotspot.png`
- `proof_pack/inspector.png`
- `proof_pack/attacks.csv`
